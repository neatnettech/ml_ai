// Exercise 34.1 — the LIBRARY translation unit (compiled into libtemp.a).
//
// TODO: #include "ex1_celsius.h" and DEFINE c_to_f here:
//       double c_to_f(double celsius) { return celsius * 9.0 / 5.0 + 32.0; }
//
// Leaving the definition out (or only declaring it) is exactly how you get an
// "Undefined symbols" linker error — that's the subject of Exercise 34.2.

#include "ex1_celsius.h"

// TODO: define c_to_f here, then delete this placeholder:
//       double c_to_f(double celsius) { return celsius * 9.0 / 5.0 + 32.0; }
void ex1_todo(void) { /* placeholder so the archive is non-empty before you start */ }
