# Core data science
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
seaborn>=0.12

# Machine learning
scikit-learn>=1.3
scipy>=1.10            # mask post-processing (Modules 11, 16)

# Image / computer vision (Advanced track, Modules 11–16)
Pillow>=10.0
opencv-python>=4.8     # seamless (Poisson) compositing — Module 16

# Deep learning
torch>=2.0
torchvision>=0.15

# Gradient boosting (optional — Module 06)
xgboost>=1.7

# NLP (optional — Module 09 bonus section)
# transformers>=4.30

# Generative vision — GPU/Colab only (Modules 14–16). Uncomment on a GPU runtime.
# transformers>=4.40
# diffusers>=0.27        # Stable Diffusion inpainting / ControlNet — Module 15/16
# accelerate>=0.27
# safetensors>=0.4
# face-alignment>=1.4    # 68-pt landmarks for FFHQ alignment — Module 16
# ninja                  # builds StyleGAN2 custom CUDA ops — Module 14

# Jupyter
jupyterlab>=4.0
ipywidgets>=8.0
ipykernel>=6.0

# Zed REPL — pairs notebooks with .py percent-format twins
jupytext>=1.16
