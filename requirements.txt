# Core data science
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
seaborn>=0.12

# Machine learning
scikit-learn>=1.3
scipy>=1.10            # mask post-processing (Modules 11, 16)

# Image / computer vision (Advanced track, Modules 11–16)
Pillow>=10.0
opencv-python>=4.8     # seamless (Poisson) compositing — Module 16

# Deep learning
torch>=2.0
torchvision>=0.15

# Gradient boosting (optional — Module 06)
xgboost>=1.7

# NLP (optional — Module 09 bonus section)
# transformers>=4.30

# Generative vision — GPU/Colab only (Modules 14–16). Uncomment on a GPU runtime.
# transformers>=4.40
# diffusers>=0.27        # Stable Diffusion inpainting / ControlNet — Module 15/16
# accelerate>=0.27
# safetensors>=0.4
# face-alignment>=1.4    # 68-pt landmarks for FFHQ alignment — Module 16
#                        #   (also works on CPU — install locally for real alignment)
# ninja                  # builds StyleGAN2 custom CUDA ops — Module 14

# Jupyter
jupyterlab>=4.0
ipywidgets>=8.0
ipykernel>=6.0

# Zed REPL — pairs notebooks with .py percent-format twins
jupytext>=1.16

# Backend track (Modules 17–20) — FastAPI, SQLAlchemy, auth/JWT, model serving
fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
alembic>=1.13            # database migrations — Module 17
pydantic>=2.6
pydantic-settings>=2.2
bcrypt>=4.1             # password hashing — Module 19
pyjwt>=2.8              # JWT sign/verify (lighter than python-jose)
cryptography>=42.0       # symmetric/asymmetric crypto — Module 18
python-multipart>=0.0.9  # OAuth2 password-form parsing — Module 19
httpx>=0.27             # test client / example calls
joblib>=1.3             # model persistence — Module 20

# Agents & Tooling (Module 27) — Model Context Protocol
mcp>=1.2               # official MCP Python SDK (FastMCP server + stdio client)

# CS Foundations bridge (Module 45) — C/C++ <-> Python extensions
pybind11>=2.12         # build the C++ extension; the ctypes/C-API bridges need no extra dep
#                        (Modules 28-44 are C/C++ + Makefile, no Python deps)

# Security track (Modules 21–26) — white-hat / network / web app security
scapy>=2.5              # packet craft & sniffing — Modules 21, 25 (sniffing needs sudo)
requests>=2.31          # HTTP exploitation against the vulnlab target — Modules 23–26
pyotp>=2.9              # TOTP multi-factor auth — Module 24
# cryptography (Module 18, above) is reused for the crypto-attack demos in Module 25.
# External tools used as concepts (install separately): nmap, Wireshark/tcpdump, Burp Suite.
